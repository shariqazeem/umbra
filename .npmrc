# The repo root is also a Next.js app (React 19 / Next 15). Keep peer-dependency
# resolution lenient (npm-style) so the benchmark workspace installs cleanly
# alongside the product app without blocking on app-side peer ranges.
auto-install-peers=true
strict-peer-dependencies=false
# Deterministic, reproducible installs for benchmark provenance.
resolution-mode=highest
