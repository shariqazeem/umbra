// Ambient module shims for packages that ship without TypeScript declarations.
// `tailwindcss-animate` is a runtime-only Tailwind plugin with no bundled types.
declare module "tailwindcss-animate";
